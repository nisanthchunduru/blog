version: "3"

tasks:
  build-backend:
    desc: "Compile backend TypeScript, build assets, and package into Lambda zip"
    cmds:
      - rm -rf backend/dist
      - docker build -f Dockerfile.backend-builder --output backend/dist .
      - zip -r backend/dist/lambda.zip config/

  terraform-apply:
    desc: "Apply Terraform to provision/update AWS infrastructure"
    dir: infra
    dotenv: ['../.env']
    cmds:
      - terraform init
      - terraform apply -auto-approve
        -var="notion_token=${NOTION_TOKEN}"
        -var="content_notion_url=${CONTENT_NOTION_URL}"
        -var="terraform_state_bucket=${TERRAFORM_STATE_BUCKET}"

  sync-frontend-to-s3:
    desc: "Sync built static assets to S3 bucket"
    cmds:
      - aws s3 sync backend/dist/public/ s3://blog-frontend-bucket-607420117092 --delete

  invalidate-frontend-cloudfront-distribution:
    desc: "Invalidate CloudFront cache after upload"
    dir: infra
    cmds:
      - bash -c 'DISTRIBUTION_ID=$(terraform output -raw cloudfront_distribution_id) && aws cloudfront create-invalidation --distribution-id "$DISTRIBUTION_ID" --paths "/*"'

  run:
    desc: "Deploy to AWS"
    cmds:
      - task: build-backend
      - task: terraform-apply
      - task: sync-frontend-to-s3
      - task: invalidate-frontend-cloudfront-distribution
